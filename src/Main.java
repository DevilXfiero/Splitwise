public class Main {
    public static void main(String[] args) {
        System.out.println("Hello world!");
        // user - add expense, settle expense, delete expense , list of all expense

        // Entities
        // User (id, name, email, List<Expense> expenseList)
        // Group (id, name, list<users>>
        // Expense(id, amount, name, isSettled, expenseType)
        // expenseType enum {


        // Services
        // User Service : Add User, EditUser, Update, Delete, getUser
        // ExpenseMgr : addExpense, settleExpense, updateExpense, deleteExpense, viewExpense(userId)



    }
}